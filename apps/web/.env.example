IDENTITY_SERVICE_URL=http://localhost:8080

# 대시보드·집계 BFF 업스트림 (API Gateway). Identity 와 같은 포트를 쓰면 안 되므로,
# 호스트에서 Identity 가 8080 이면 Compose 에서 API_GATEWAY_PORT=8088 등으로 분리 후 여기도 맞춘다.
# docs/contracts/web-gateway-bff.md 참고
API_GATEWAY_URL=http://localhost:8080
