# team-service `web-mfe` — Module Federation 리모트(TeamManagement)
# 이 리모트의 정적·청크 URL 접두. web-host의 `NEXT_PUBLIC_MFE_TEAM_REMOTE_URL`과 맞아야 함
# (예: 호스트가 `http://localhost:3012/mfe/team` 이면 여기는 `/mfe/team`).
# 설정하지 않으면 코드 기본값도 `/mfe/team` 입니다.
NEXT_PUBLIC_MFE_ASSET_PREFIX=/mfe/team

# BFF API upstream
# GATEWAY_URL=http://localhost:8080
# WEB_GATEWAY_URL=http://localhost:8080
# IDENTITY_SERVICE_URL=http://localhost:8082
