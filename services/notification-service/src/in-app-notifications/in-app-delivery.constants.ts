/** Persisted `notificationDelivery.channel` for in-app notifications. */
export const IN_APP_DELIVERY_CHANNEL = 'in-app';

/** Persisted `notificationDelivery.status` when delivery is recorded at write time. */
export const IN_APP_DELIVERY_STATUS = 'delivered';
