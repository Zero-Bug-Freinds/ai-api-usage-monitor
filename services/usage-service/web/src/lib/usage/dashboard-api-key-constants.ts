/** Matches personal/team dashboard usage filters — omit server param when aggregating all keys. */
export const DASHBOARD_API_KEY_ALL = "__ALL__"

/** 개인 키 목록이 비었을 때만 사용(서버 미전달과 동일 의미이나 UI에서 ‘없음’ 표시). */
export const DASHBOARD_API_KEY_NONE = "__NONE__"
